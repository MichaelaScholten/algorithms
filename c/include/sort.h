#pragma once

#include "data.h"

/// @brief Sorts the passed data
/// @param data the data to sort
void bubble_sort(Vec *data);

/// @brief Sorts the passed data
/// @param data the data to sort
void insertion_sort(Vec *data);

/// @brief Sorts the passed data
/// @param data the data to sort
void merge_sort(Vec *data);

/// @brief Sorts the passed data
/// @param data the data to sort
void selection_sort(Vec *data);
